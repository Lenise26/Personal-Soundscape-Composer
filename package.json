{
  "name": "personal-soundscape-composer",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production-quality browser soundscape composer for small creative businesses and relaxation workflows.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/audioEngine.js && node --check src/data.js && node --check src/storage.js && node --check src/format.js",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
